# Memory Heap

The Memory Heap stores variables, objects, arrays, and functions.

Example:

```javascript
let name = "Hamza";
let age = 22;
```

Think of it as a warehouse where data is stored for the runtime to reference.
